apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller
  namespace: test
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL
      - image: registry.ske.eu01.stackit.cloud/stackit/gardener-project/gardener/etcd-druid:v0.3.1
        name: druid
